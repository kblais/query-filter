includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - src
        - tests

    # The level 8 is the highest level
    level: 5

    parallel:
        processTimeout: 300.0 # Fix timeouts in Gitlab CI
